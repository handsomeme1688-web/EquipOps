<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zoee.equipops.system.mapper.PermissionMapper">
    <select id="selectPermissionCodesByUser" resultType="java.lang.String">
        SELECT DISTINCT p.code FROM user_role ur
        LEFT JOIN role_permission rp  ON ur.role_id = rp.role_id
        LEFT JOIN permission p ON rp.permission_id = p.id
        <where>
            ur.user_id = #{userId}
        </where>
    </select>
</mapper>
